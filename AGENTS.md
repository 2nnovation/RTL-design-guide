# AGENTS.md

## 저장소 목적

이 저장소는 디지털 RTL 엔지니어를 위한 공개 실무형 **RTL Design & Optimization Guide**입니다. 이 가이드는 독자가 아키텍처, 타이밍, 전력, 면적, 클록, 리셋, CDC, 합성, 물리 구현, 검증 관점에서 설계 리뷰 결정을 내릴 수 있도록 도와야 합니다.

핵심 원칙은 다음과 같습니다.

> RTL은 단순히 올바르게 동작하는 코드를 작성하는 것이 아니다. RTL은 의도한 하드웨어 아키텍처를 기술한다.

다음 최적화 순서를 기본 의사결정 프레임워크로 사용합니다.

> 제거(Remove) → 비활성화(Disable) → 단순화(Simplify) → 상황에 따라 공유 또는 복제(Share or Duplicate) → 파이프라인 또는 MCP(Pipeline or MCP) → 물리 최적화(Physical optimization)

## 지침 적용 범위

이 지침은 저장소 전체에 적용됩니다. 향후 워크플로에 더 좁은 범위의 규칙이 필요하면 하위 디렉터리에 더 구체적인 `AGENTS.md` 파일을 추가할 수 있습니다.

활성 역할은 작업 프롬프트가 결정합니다. 한 역할에서 다른 역할로 범위를 암묵적으로 확장하지 마십시오.

### 기술 문서 작성자 역할

기술 내용을 작성하거나 개정하는 작업에서는 다음을 따릅니다.

- 개요나 키워드 목록이 아니라 읽기 쉽고 기술적으로 유용한 Markdown을 작성합니다.
- 관련되는 경우 해당 주제가 중요한 이유, 하드웨어 구조, 예상 합성 매핑, PPA 영향, 트레이드오프, 실패 모드, 권장 패턴, 설계 리뷰 질문을 설명합니다.
- 짧고 일반적인 SystemVerilog 예제와 직접 작성한 ASCII 또는 Mermaid 다이어그램을 사용합니다.
- 합성, STA, CDC 또는 P&R 결과가 도구, 제약 조건, 라이브러리, 공정 기술 또는 물리 구현에 따라 달라지는 경우 단정적인 주장을 피합니다.
- 가이드 페이지는 상세하게 유지하고, 저장소 소개 페이지인 `README.md`는 간결하게 유지합니다.
- 긴 내용을 여러 곳에 중복하는 대신 관련 주제를 서로 링크합니다.

### 저장소 관리자 역할

저장소 통합, 검증 또는 공개 작업에서는 다음을 따릅니다.

- 작성된 문서의 기술적 의미를 보존합니다.
- 작업에서 편집을 명시적으로 요청하지 않는 한 도메인 주장을 축약하거나 다시 쓰거나 "개선"하지 않습니다.
- 내비게이션, 링크, 형식, 빌드 호환성, 일관성을 위해 필요한 소규모의 기계적인 수정만 합니다. 기술적 우려가 있으면 조용히 변경하지 말고 보고합니다.
- `mkdocs.yml`, 내비게이션, 의존성, Git 위생, 배포 구성을 관리합니다.
- 통합 후 전체 문서 사이트를 검증합니다.
- 커밋을 제안하기 전에 전체 diff를 검토합니다.
- 요청받은 경우에만 커밋을 준비합니다. Push, 공개, 배포, Pull Request 생성 또는 그 밖의 외부 서비스 변경은 사용자의 명시적 승인을 받은 후에만 수행합니다.

## 공개 저장소 안전성

이곳에 커밋되는 모든 내용은 공개 저장소에 올려도 안전해야 합니다.

다음 내용을 절대 추가하지 마십시오.

- 회사 내부 아키텍처, 플로, 방법론, 문서 또는 코드
- 실제 제품, 고객, 프로젝트 또는 내부 IP 이름
- 실제 내부 신호 이름 또는 식별 가능한 실패 보고서
- 기밀 타이밍, 전력, 면적, 공정 또는 구현 데이터
- 자격 증명, 토큰, 비공개 URL, 로컬 사용자 이름 또는 머신별 절대 경로

공개된 설계 원칙, 직접 작성한 일반 예제, 명백히 가상인 수치만 사용합니다. 공개하기 전에 staged diff에서 비밀정보, 식별 정보, 실수로 포함된 로컬 산출물을 검사합니다.

## 소스 및 생성 파일

- `docs/`는 문서의 원본 소스입니다.
- `mkdocs.yml`에는 사이트 구성과 내비게이션이 들어 있습니다.
- `README.md`는 GitHub 소개 페이지이며 전체 가이드가 아닙니다.
- `site/`는 생성된 결과물입니다. 직접 수정하거나 커밋하지 마십시오.
- `.venv/`, 캐시, 편집기 메타데이터, 로컬 빌드 산출물은 추적하지 않은 상태로 유지해야 합니다.

생성된 결과물이 `docs/`와 일치하지 않으면 소스 또는 빌드 구성을 수정한 뒤 다시 빌드합니다.

## 문서 작성 규칙

- SystemVerilog 문법을 알고 있으며 합성, 타이밍, 전력, CDC, 검증에 관한 실무 판단을 원하는 RTL 엔지니어를 주 독자로 삼습니다.
- 문제 → 하드웨어 관점 → RTL 예제 → 합성 관점 → PPA 영향 → 트레이드오프 → 리뷰 질문의 흐름을 우선합니다.
- inferred clock gating, explicit clock gating, multi-cycle path, bundled data, synchronizer와 같은 정확한 용어를 사용합니다.
- 순차 RTL 예제에서는 이벤트 우선순위를 명시적으로 밝힙니다.
- CDC 예제에서는 소스 안정성, 목적지 캡처, 펄스 폭, 일관성, 리셋 동작에 관한 가정을 명시합니다.
- MCP 예제에서는 기능적 multi-cycle 의도와 단순히 위반을 숨기기 위해 사용하는 제약 조건을 구분하고 setup/hold 관계를 설명합니다.
- 읽기 쉬운 RTL 문법이 좋은 하드웨어를 보장하거나 최적화에 비용이 들지 않는다고 암시하지 않습니다.
- 제목 수준, 상대 링크, 코드 펜스, 용어를 인접한 페이지와 일관되게 유지합니다.

## 필수 저장소 점검

저장소 관리 작업을 인계하기 전에 다음을 수행합니다.

1. `mkdocs.yml`에서 참조하는 모든 페이지가 존재하는지 확인합니다.
2. 변경된 Markdown에서 깨진 상대 링크와 잘못된 코드 펜스를 확인합니다.
3. 저장소 루트에서 strict 모드로 빌드합니다.

   ```powershell
   .\.venv\Scripts\python.exe -m mkdocs build --strict
   ```

   기존 `site/` 디렉터리가 미리보기 프로세스에 의해 잠겨 있으면 강제로 삭제하지 마십시오. 새 임시 디렉터리를 만들고 `--site-dir <temporary-path>`를 사용해 빌드한 뒤 잠금 상태를 보고합니다. 기존 가상 환경을 사용할 수 없다면 의존성을 설치하거나 변경하기 전에 그 사실을 보고합니다.

4. 관련 없는 사용자 변경을 보존하면서 Git 상태와 diff를 검사합니다.
5. `site/`, `.venv/`, 캐시, 비밀정보, 머신별 파일이 staged 상태가 아닌지 확인합니다.
6. 변경된 파일, 검증 결과, 남은 경고, 외부 작업에 아직 승인이 필요한지를 요약합니다.

## Git 안전성

- 사용자가 다른 브랜치나 worktree를 요청하지 않는 한 현재 checkout과 브랜치에서 작업합니다.
- 관련 없는 변경을 절대 폐기하지 않습니다.
- hard reset, 강제 checkout, clean 작업과 같은 파괴적인 이력 또는 working tree 명령을 사용하지 않습니다.
- 명시적으로 요청받지 않는 한 amend, rebase, force-push, tag 또는 이력 재작성을 수행하지 않습니다.
- 검토가 더 명확해진다면 콘텐츠 변경과 인프라 변경을 분리합니다.
- 생성된 `site/` 결과물을 커밋하지 않습니다.

## 협업 및 인계

콘텐츠 작성과 저장소 관리는 별도의 Codex 작업에서 진행할 수 있지만, 동일한 checkout에서 같은 파일을 동시에 수정해서는 안 됩니다. 한 변경 세트를 완료하고 검토한 후 다음 변경을 통합하거나, 명시적으로 지정된 브랜치/worktree를 사용합니다.

저장소 관리자 인계 보고에는 다음 내용이 포함되어야 합니다.

- 통합한 내용
- 변경된 파일
- 통과하거나 실패한 점검
- 작성자가 검토해야 할 기술 콘텐츠 관련 질문
- 제안하는 커밋 경계
- Push 또는 배포 승인이 필요한지 여부
